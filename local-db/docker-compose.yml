services:
  next_db:
    container_name: next_db
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: "next_db"
      POSTGRES_HOST_AUTH_METHOD: "trust"
    ports:
      - "5432:5432"
